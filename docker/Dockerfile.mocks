FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache curl

# Create a simple mock server
RUN npm init -y && npm install express cors body-parser

# Create the mock server
COPY mock-server.js /app/server.js

CMD ["node", "server.js"]